Both confirmed: **Kind + APIVersion** in the ref, fabric uses **InternalNode**. This makes the GC fully generic — it builds an unstructured object from the ref's GVK and does a single `GET`, no per-kind branching. Here's the revised plan (deltas from the approved version in **bold**).

## Goal
Replace `TargetID` with `TargetRef`, a typed object reference: **`APIVersion` + `Kind`** + required `Name` + optional `Namespace`. Gateway bindings → `{v1, Pod, podName, podNs}`; fabric bindings → `{networking.liqo.io/v1beta1, InternalNode, nodeName, ""}`. The GC resolves/GETs the target generically from its GVK; the fabric/gateway pods filter bindings in `SetupWithManager` by **Kind + Name + Namespace**.

## Constraints
- Bindings stay co-located with their `FirewallConfiguration` (ownerRef cascade + `Owns()` preserved; no cross-namespace ownerRef).
- `APIVersion`, `Kind`, `Name` required; `Namespace` optional (empty ⇒ cluster-scoped).
- Preserve nftables cleanup semantics (pod cleans on delete; GC force-removes only when target gone).
- No new external dependencies; regenerate CRD/deepcopy/RBAC via `make`.
- Clean break, no `targetID` migration.

## Target ref values
| Creator | APIVersion | Kind | Name | Namespace |
|---|---|---|---|---|
| gateway | `v1` | `Pod` | `GwOptions.PodName` | `GwOptions.Namespace` |
| fabric | `networking.liqo.io/v1beta1` | `InternalNode` | `options.NodeName` | empty |

## Chunks

### Chunk 1 — API: `TargetReference` type **(with GVK)**
- **Files**: `apis/networking/v1beta1/firewallconfigurationbinding_types.go`, `zz_generated.deepcopy.go` (regen), CRD YAML (regen).
- `TargetReference{ APIVersion string \`json:"apiVersion"\`; Kind string \`json:"kind"\`; Name string \`json:"name"\`; Namespace string \`json:"namespace,omitempty"\` }`; replace `TargetID` with `TargetRef`; printcolumns → `TargetKind`/`TargetName`/`TargetNamespace`.
- **Accept When**: CRD shows required `apiVersion`/`kind`/`name`, optional `namespace`; `GOOS=linux go build ./apis/...` passes.

### Chunk 2 — Creators set `TargetRef`
- **Files**: `pkg/firewall/binding_creator_base.go` (`BindingTarget` gains `APIVersion`, `Kind`, `Namespace`; `ensureBinding` sets `TargetRef`; keep `SetControllerReference`), `pkg/fabric/binding_creator_controller.go` (InternalNode ref), `pkg/gateway/binding_creator_controller.go` (Pod ref; constructor gains namespace), `cmd/gateway/main.go`.
- **Accept When**: gateway/fabric bindings carry the correct GVK+name+namespace; build passes.
- **Test Coverage**: update creator/base tests.

### Chunk 3 — Pod reconciler predicate + watch + cleanup filter by **Kind+Name+Namespace**
- **Files**: `pkg/firewall/firewallconfigurationbinding_controller.go` (`ForgeTargetRefPredicate(apiVersion, kind, name, namespace)`; `SetupWithManager` takes these), `pkg/firewall/watch.go` (`NewFirewallBindingWatchEventHandler` filters by ref — fixes the latent label no-op), `pkg/firewall/cleanup.go` (match by ref), `cmd/fabric/main.go`, `cmd/gateway/main.go`.
- **Accept When**: each pod reconciles only bindings whose `TargetRef` matches its own Kind+Name+Namespace; build + lint pass.
- **Test Coverage**: update predicate tests + `cleanup_test.go`.

### Chunk 4 — GC generic target resolution **(no pod/internalnode branching)**
- **Files**: `pkg/liqo-controller-manager/networking/firewall/bindinggc_controller.go`, `.../doc.go`.
- `isTargetLinked(ctx, ref)`: build `*unstructured.Unstructured`, set `GroupVersionKind` from `ref.APIVersion`+`ref.Kind`, `GET {ref.Namespace, ref.Name}` — linked iff it exists (IsNotFound ⇒ not linked; other errors propagate). **Removes** the pod-vs-InternalNode custom logic.
- Watches: keep concrete `Watches(&corev1.Pod{})` and `Watches(&networkingv1beta1.InternalNode{})` (controller-runtime needs concrete types at setup — these are the kinds actually referenced); `enqueueBindingsByTarget` matches bindings whose `TargetRef` GVK+Name+Namespace equals the event object's.
- RBAC: pods `get;list;watch`, internalnodes `get;list;watch` (unchanged); no `nodes` needed.
- **Accept When**: a binding whose Pod exists (right ns) is kept; a binding whose target is gone is Delete + finalizer-removed; generic GET path exercised for both kinds; lint clean.
- **Test Coverage**: GC test with a Pod-kind ref (present/absent) and an InternalNode-kind ref (present/absent).

### Chunk 5 — Regenerate + full verification
- **Files**: regenerated ClusterRoles + CRD/deepcopy.
- **Accept When**: `make rbacs manifests generate` only intended diffs; `GOOS=linux go build ./...`, `go vet`, `golangci-lint run` clean; `go test -c ./pkg/firewall/... ./pkg/liqo-controller-manager/networking/firewall/...` compiles.

## Decision Log (updated)
- **Ref carries full GVK** (`APIVersion`+`Kind`) → GC resolves any target type via unstructured GET; no per-kind switch. Rejected Kind-only (ambiguous group; would need an internal kind→GVK table).
- **Fabric Kind = InternalNode** (`networking.liqo.io/v1beta1`); gateway Kind = Pod (`v1`).
- **GC watches stay concrete** for Pod + InternalNode (controller-runtime requires concrete types for `Watches`); only the *link check* is generic. Enqueue matches by GVK+Name+Namespace.
- Everything else unchanged: bindings co-located with fwcfg; ownerRef cascade preserved; nft-monitor watch switched to ref match; clean break, no migration.

## Risks
- **Generic GET needs the ref's GVK registered in the client scheme** (low): `v1/Pod` and `networking.liqo.io/v1beta1/InternalNode` are both in the manager scheme already; unstructured GET works regardless. I'll verify during Chunk 4.
- **Uncached unstructured GET** (low): may bypass the informer cache; acceptable for a 10-min GC path. Will confirm no RBAC gap.
- Gateway pod namespace assumption (low, verified); existing-binding churn on upgrade (accepted).

All open questions resolved. Ready to implement on approval.

<!-- PLAN_COMPLETE -->