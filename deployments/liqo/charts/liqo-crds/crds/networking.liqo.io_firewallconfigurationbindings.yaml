---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: firewallconfigurationbindings.networking.liqo.io
spec:
  group: networking.liqo.io
  names:
    categories:
    - liqo
    kind: FirewallConfigurationBinding
    listKind: FirewallConfigurationBindingList
    plural: firewallconfigurationbindings
    shortNames:
    - fwbinding
    - fwcfgbinding
    singular: firewallconfigurationbinding
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Applied')].status
      name: Applied
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .spec.firewallConfigurationRef.name
      name: FirewallConfiguration
      priority: 1
      type: string
    - jsonPath: .spec.targetRef.kind
      name: TargetKind
      priority: 1
      type: string
    - jsonPath: .spec.targetRef.name
      name: TargetName
      priority: 1
      type: string
    - jsonPath: .spec.targetRef.namespace
      name: TargetNamespace
      priority: 1
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: |-
          FirewallConfigurationBinding links an entity (e.g. a fabric pod or gateway) to a FirewallConfiguration.
          The entity that owns this resource is responsible for applying the referenced FirewallConfiguration
          and for cleaning up the nftables rules when this resource is deleted.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FirewallConfigurationBindingSpec defines the desired state
              of FirewallConfigurationBinding.
            properties:
              firewallConfigurationRef:
                description: FirewallConfigurationRef is the reference to the FirewallConfiguration
                  to apply.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              targetRef:
                description: |-
                  TargetRef identifies the entity (e.g. a gateway pod or an InternalNode) that should apply this binding.
                  The FirewallConfigurationBinding controller running on the matching entity filters resources by this field.
                  It must be unique across all FirewallConfigurationBinding controllers instances in the cluster,
                  otherwise multiple entities will apply the same FirewallConfiguration, which may cause unexpected behavior.
                properties:
                  apiVersion:
                    description: |-
                      APIVersion of the referenced target (e.g. "v1" for a Pod or
                      "networking.liqo.io/v1beta1" for an InternalNode).
                    type: string
                  kind:
                    description: Kind of the referenced target (e.g. "Pod" or "InternalNode").
                    type: string
                  name:
                    description: Name of the referenced target.
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referenced target. It must be empty for cluster-scoped targets
                      (e.g. an InternalNode) and set for namespaced targets (e.g. a Pod).
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
            required:
            - firewallConfigurationRef
            - targetRef
            type: object
          status:
            description: FirewallConfigurationBindingStatus defines the observed state
              of FirewallConfigurationBinding.
            properties:
              conditions:
                description: Conditions contains the conditions of the FirewallConfigurationBinding.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              tableName:
                description: |-
                  TableName is the name of the nftables table managed by this binding.
                  Cached here so that cleanup can proceed even after the FirewallConfiguration is deleted.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
