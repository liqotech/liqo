---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: internalfabrics.networking.liqo.io
spec:
  group: networking.liqo.io
  names:
    categories:
    - liqo
    kind: InternalFabric
    listKind: InternalFabricList
    plural: internalfabrics
    singular: internalfabric
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.nodeName
      name: Gateway Node
      type: string
    - jsonPath: .spec.endpoint.ip
      name: Gateway IP
      type: string
    - jsonPath: .spec.endpoint.port
      name: Gateway Port
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InternalFabric contains the network internalfabric of a pair
          of clusters, including the local and the remote pod and external CIDRs and
          how the where remapped.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: InternalFabricSpec defines the desired state of InternalFabric.
            properties:
              gatewayAddress:
                description: GatewayAddress is the address of the gateway pod
                type: string
              interface:
                description: Interface contains the information about network interfaces.
                properties:
                  gateway:
                    description: Gateway contains the information about the gateway
                      interface.
                    properties:
                      name:
                        description: Name is the name of the gateway interface.
                        type: string
                    required:
                    - name
                    type: object
                required:
                - gateway
                type: object
              mtu:
                description: MTU is the MTU of the internal fabric.
                type: integer
              remoteCIDRs:
                description: RemoteCIDRs is the list of remote CIDRs to be routed
                  through the gateway.
                items:
                  description: CIDR defines a syntax validated CIDR.
                  pattern: ^(([1-9]{0,1}[0-9]{0,2}|2[0-4][0-9]|25[0-5])\.){3}([1-9]{0,1}[0-9]{0,2}|2[0-4][0-9]|25[0-5])\/([0-9]|[1-2][0-9]|3[0-2])$
                  type: string
                type: array
            required:
            - gatewayAddress
            - interface
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
