---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: routes.networking.liqo.io
spec:
  group: networking.liqo.io
  names:
    categories:
    - liqo
    kind: Route
    listKind: RouteList
    plural: routes
    singular: route
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.dest.ip
      name: Destination IP
      type: string
    - jsonPath: .spec.dest.cidr
      name: Destination CIDR
      type: string
    - jsonPath: .spec.nextHop.ip
      name: Next Hop IP
      priority: 1
      type: string
    - jsonPath: .spec.nextHop.dev
      name: Next Hop Dev
      priority: 1
      type: string
    - jsonPath: .status.conditions[?(@.type == 'Applied')].status
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Route contains the network route of a pair of clusters, including
          the local and the remote pod and external CIDRs and how the where remapped.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: RouteSpec defines the desired state of Route.
            properties:
              dest:
                description: Dest is the destination of the route.
                properties:
                  cidr:
                    description: CIDR is the CIDR of the destination. It is mutually
                      exclusive with IP.
                    type: string
                  ip:
                    description: IP is the IP address of the destination. It is mutually
                      exclusive with CIDR.
                    type: string
                type: object
              nextHop:
                description: NextHop is the next hop of the route.
                properties:
                  dev:
                    description: Dev is the name of the device of the next hop. It
                      is mutually exclusive with IP.
                    type: string
                  ip:
                    description: IP is the IP address of the next hop. It is mutually
                      exclusive with Dev.
                    type: string
                type: object
              table:
                description: Table is the table of the route.
                type: string
            type: object
          status:
            description: RouteStatus defines the observed state of Route.
            properties:
              conditions:
                description: Conditions contains information about the current status
                  of the route.
                items:
                  description: RouteCondition contains details for the current condition
                    of this route.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human-readable message indicating
                        details about last transition.
                      type: string
                    reason:
                      description: Reason is a unique, one-word, CamelCase reason
                        for the condition's last transition.
                      type: string
                    status:
                      default: Unknown
                      description: Status is the status of the condition.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: Type is the type of the condition.
                      enum:
                      - Applied
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
