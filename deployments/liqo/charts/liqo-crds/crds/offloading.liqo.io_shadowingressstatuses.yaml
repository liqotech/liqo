---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: shadowingressstatuses.offloading.liqo.io
spec:
  group: offloading.liqo.io
  names:
    categories:
    - liqo
    kind: ShadowIngressStatus
    listKind: ShadowIngressStatusList
    plural: shadowingressstatuses
    shortNames:
    - shis
    singular: shadowingressstatus
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: ShadowIngressStatus is the Schema for the ShadowIngressStatus
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ShadowIngressStatusSpec defines the desired state of ShadowIngressStatus.
            properties:
              clusterID:
                description: ClusterID is the ID of the remote cluster that owns this
                  status.
                type: string
              ingressName:
                description: IngressName is the name of the local Ingress whose remote
                  status is reported.
                type: string
              loadBalancer:
                description: LoadBalancer is the load-balancer status of the remote
                  Ingress.
                properties:
                  ingress:
                    description: ingress is a list containing ingress points for the
                      load-balancer.
                    items:
                      description: IngressLoadBalancerIngress represents the status
                        of a load-balancer ingress point.
                      properties:
                        hostname:
                          description: hostname is set for load-balancer ingress points
                            that are DNS based.
                          type: string
                        ip:
                          description: ip is set for load-balancer ingress points
                            that are IP based.
                          type: string
                        ports:
                          description: ports provides information about the ports
                            exposed by this LoadBalancer.
                          items:
                            description: IngressPortStatus represents the error condition
                              of a service port
                            properties:
                              error:
                                description: |-
                                  error is to record the problem with the service port
                                  The format of the error shall comply with the following rules:
                                  - built-in error values shall be specified in this file and those shall use
                                    CamelCase names
                                  - cloud provider specific error values must have names that comply with the
                                    format foo.example.com/CamelCase.
                                maxLength: 316
                                pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                                type: string
                              port:
                                description: port is the port number of the ingress
                                  port.
                                format: int32
                                type: integer
                              protocol:
                                description: |-
                                  protocol is the protocol of the ingress port.
                                  The supported values are: "TCP", "UDP", "SCTP"
                                type: string
                            required:
                            - error
                            - port
                            - protocol
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                type: object
            required:
            - clusterID
            - ingressName
            type: object
        type: object
    served: true
    storage: true
