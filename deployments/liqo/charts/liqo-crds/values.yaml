# CRD Upgrade Configuration
# Controls whether CRDs should be automatically updated during helm upgrade
crdUpgrade:
  # enabled determines if the pre-upgrade hook should run to update CRDs
  enabled: false
  # keepResources determines if the hook resources (Job, ConfigMap, ServiceAccount, ClusterRole, ClusterRoleBinding)
  # should be kept after the hook completes. If false, they are deleted on success.
  keepResources: false
  # image is the CRD upgrade container image
  # NOTE: This image only supports linux/amd64 and linux/arm64 platforms (arm/v7 is not supported).
  image:
    name: "us-docker.pkg.dev/castai-hub/library/liqo/liqo-crd-upgrade"
    # version: REQUIRED when crdUpgrade.enabled is true. The image tag/version to use.
    # Set using: --set liqo-crds.crdUpgrade.image.version=<version>
    version: ""
    pullPolicy: IfNotPresent
